# Documentation Index

This index links to key documentation files in the repository.

- `ARCHITECTURE.md`: System architecture overview and testing strategy.
- `payment_service.md`: Domain documentation for the Payment service.
- `wallet_service.md`: Domain documentation for the Wallet service.
- `payment_repository.md`: Repository documentation for Payment queries.
- `wallet_repository.md`: Repository documentation for Wallet queries.
- `payment_gateway_endpoints.md`: API overview for payments (if present).
- `health_endpoint.md`, `wallets_request.md`, `incoming_sms.md`, `payments.md`: Endpoint docs.
- `dependencies_auth.md` – describes authentication dependencies (`deps.get_current_company`, `deps.get_current_channel`).
- `sms_service.md` – describes SMS integration and the SMS service responsibilities and data flow.
- `app/core/logging_config.py` – Logging configuration used by the application.
- `app/core/exceptions.py` – Global exception handler and error handling notes.

Ensure these docs are kept in sync with the code and linked from the ARCHITECTURE overview.

Add more docs under this directory as the project grows.
